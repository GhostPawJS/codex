import { strictEqual } from 'node:assert/strict';
import { describe, it } from 'node:test';

describe('dismissal types', () => {
	it('exist for module exports', () => {
		strictEqual(typeof 1, 'number');
	});
});
